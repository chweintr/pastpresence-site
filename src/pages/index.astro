---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navigation from '../components/Navigation.astro';
import ParticleBackground from '../components/ParticleBackground.astro';
import {
  architectureSteps,
  company,
  productLines,
  tractionStats,
} from '../data/company';
---

<BaseLayout title="Home" description="PastPresence builds authored conversational AI across HEARSAY, Echoes, and Reading Companions.">
  <ParticleBackground />
  <Navigation />

  <section class="relative min-h-screen flex items-center pt-24 pb-16 px-6">
    <div class="max-w-7xl mx-auto w-full">
      <div class="grid lg:grid-cols-[1.2fr_1fr] gap-10 items-center">
        <div>
          <p class="uppercase tracking-[0.24em] text-xs text-white/80 mb-4">{company.name}</p>
          <h1 class="text-5xl md:text-7xl font-display leading-tight text-white mb-6">
            Live, interactive, face-to-face conversations
            <span class="block text-gradient text-2xl md:text-4xl font-light mt-3">
              with fictional characters, historical figures, mythical creatures, and living legends.
            </span>
          </h1>
          <p class="text-xl text-white/85 max-w-2xl mb-8">
            Writers define how characters think, speak, and respond. The AI performs within those constraints.
            Nothing is scripted. Everything is designed.
          </p>
          <div class="flex flex-wrap gap-4">
            <a href="/portfolio" class="glass-mint border border-white/20 hover:bg-white/10 px-7 py-3 rounded-full font-medium text-white transition-all duration-300">
              Explore Products
            </a>
            <a href="/revenue" class="glass border border-white/20 hover:border-white/35 px-7 py-3 rounded-full font-medium text-white transition-all duration-300">
              View Business Model
            </a>
            <a href="/contact" class="bg-gradient-to-r from-accent-pink to-accent-coral hover:opacity-90 text-white px-7 py-3 rounded-full font-medium transition-all duration-300">
              Contact Caleb
            </a>
          </div>
        </div>

        <div class="space-y-6">
          <div class="flex justify-center lg:justify-end">
            <div class="relative w-72 h-72 md:w-80 md:h-80">
              <div class="w-full h-full rounded-full overflow-hidden border-4 border-white/70 shadow-2xl">
                <video
                  autoplay
                  muted
                  loop
                  playsinline
                  class="w-full h-full object-cover"
                  src="/video/holographic-conversation-demo.mp4"
                ></video>
              </div>
              <div class="absolute inset-0 bg-accent-coral/20 rounded-full blur-2xl -z-10 transform scale-110"></div>
            </div>
          </div>

          <div class="glass-dark rounded-3xl p-8 text-white">
            <p class="text-sm uppercase tracking-[0.2em] text-white/60 mb-4">Problem We Solve</p>
            <ul class="space-y-4 text-sm md:text-base">
              <li class="flex items-start gap-3">
                <span class="text-accent-pink mt-1">•</span>
                <span>Most conversational AI has no authorial voice and no durable artifact.</span>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-accent-pink mt-1">•</span>
                <span>Rich conversations disappear instead of becoming stories, memory, or study objects.</span>
              </li>
              <li class="flex items-start gap-3">
                <span class="text-accent-pink mt-1">•</span>
                <span>Creators have no native medium for authored, voice-driven, real-time dialogue.</span>
              </li>
            </ul>
            <div class="mt-8 pt-6 border-t border-white/10">
              <p class="text-white/80 text-sm">{company.oneLiner}</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="relative py-20 px-6">
    <div class="max-w-7xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="text-4xl md:text-5xl font-display text-white mb-4">Three Product Lines</h2>
        <p class="text-white/75 max-w-3xl mx-auto">
          Each line serves a different market, but all use the same authored character engine and conversational infrastructure.
        </p>
      </div>
      <div class="grid lg:grid-cols-3 gap-6">
        {productLines.map((line) => (
          <article class="glass rounded-3xl p-7">
            <p class="text-xs uppercase tracking-[0.16em] text-white/70 mb-3">{line.model}</p>
            <h3 class="text-2xl text-white font-semibold mb-2">{line.name}</h3>
            <p class="text-sm text-white/70 mb-4">{line.subtitle}</p>
            <p class="text-white/85 mb-5 leading-relaxed">{line.summary}</p>
            <p class="text-sm text-accent-coral mb-4">{line.pricing}</p>
            <ul class="space-y-2 mb-6">
              {line.highlights.map((item) => (
                <li class="text-sm text-white/75 flex items-start gap-2">
                  <span class="text-accent-pink mt-1">•</span>
                  <span>{item}</span>
                </li>
              ))}
            </ul>
            <a
              href={line.primaryLink.href}
              target={line.primaryLink.href.startsWith('http') ? '_blank' : undefined}
              rel={line.primaryLink.href.startsWith('http') ? 'noopener noreferrer' : undefined}
              class="inline-flex items-center gap-2 text-white hover:text-accent-coral transition-colors"
            >
              {line.primaryLink.label}
              <span aria-hidden="true">→</span>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="relative py-20 px-6 bg-background-secondary/30">
    <div class="max-w-7xl mx-auto">
      <div class="glass rounded-3xl p-10">
        <h2 class="text-3xl md:text-4xl text-white mb-8 text-center">Authored Constraint Architecture</h2>
        <div class="grid md:grid-cols-3 gap-6">
          {architectureSteps.map((step) => (
            <div class="glass-dark rounded-2xl p-6">
              <h3 class="text-xl text-white mb-3">{step.title}</h3>
              <p class="text-white/75 text-sm leading-relaxed">{step.detail}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="relative py-20 px-6">
    <div class="max-w-7xl mx-auto">
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-5 mb-12">
        {tractionStats.map((item) => (
          <div class="glass-dark rounded-2xl p-6">
            <p class="text-xs uppercase tracking-[0.14em] text-white/60 mb-3">{item.label}</p>
            <p class="text-3xl text-white font-semibold mb-2">{item.value}</p>
            <p class="text-sm text-white/70">{item.detail}</p>
          </div>
        ))}
      </div>

      <div class="glass rounded-3xl p-10">
        <h2 class="text-3xl text-white mb-6">Work With PastPresence</h2>
        <p class="text-white/80 mb-6 max-w-3xl">
          We collaborate with creative studios, cultural institutions, educators, and strategic partners to design and deploy authored conversational experiences.
        </p>
        <div class="grid md:grid-cols-2 gap-4 mb-8">
          <div class="glass-dark rounded-2xl p-5">
            <p class="text-white text-sm font-medium mb-2">Creative + Narrative Partners</p>
            <p class="text-white/70 text-sm">Co-develop new worlds, characters, and serialized formats.</p>
          </div>
          <div class="glass-dark rounded-2xl p-5">
            <p class="text-white text-sm font-medium mb-2">Institutional and Venue Partners</p>
            <p class="text-white/70 text-sm">Build place-based conversational installations and pilots.</p>
          </div>
          <div class="glass-dark rounded-2xl p-5">
            <p class="text-white text-sm font-medium mb-2">Education Partners</p>
            <p class="text-white/70 text-sm">Pilot Reading Companions for classrooms and programs.</p>
          </div>
          <div class="glass-dark rounded-2xl p-5">
            <p class="text-white text-sm font-medium mb-2">Strategic Support</p>
            <p class="text-white/70 text-sm">Support platform growth, launch operations, and market expansion.</p>
          </div>
        </div>
        <a href="/contact" class="inline-flex items-center gap-2 bg-gradient-to-r from-accent-pink to-accent-coral hover:opacity-90 text-white px-7 py-3 rounded-full font-medium transition-all duration-300">
          Start the Conversation
          <span aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
